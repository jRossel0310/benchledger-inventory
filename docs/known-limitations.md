# Known limitations

Accepted, documented tradeoffs — not bugs. Each entry says what the gap is,
why it's acceptable for now, and what closes it.

## Import parsing (Phase 5a)

- **OCR is not implemented.** A scanned/image-only PDF (one whose text
  extraction yields ~no positioned tokens — see `crate::pdf::TextExtraction`
  in `crates/inventory-import/src/pdf/mod.rs`) produces a `ParsedInvoice`
  with no fabricated order metadata or line items and a single warning:
  "scanned PDF — OCR not yet available; use manual correction or upload
  CSV/XLSX". The real OCR implementation (planned: Windows' built-in OCR
  APIs) is deferred past 5a; 5a ships only the detection branch plus the
  low-confidence/warning signal, so 5d's manual-correction UI has a defined
  contract to build against once it exists. Until then, a scanned invoice
  must be re-exported as CSV/XLSX or entered manually.
- **`PdfiumTextSource`'s word-grouping constants are unvalidated against a
  live pdfium.** `GAP_FRACTION_OF_HEIGHT` and
  `LINE_DRIFT_FRACTION_OF_HEIGHT` (`crates/inventory-import/src/pdf/text_source.rs`)
  control when adjacent characters merge into one word versus split into
  two, for PDFs whose generator never emits an explicit space glyph. They
  are reasoned estimates (documented inline), not measured against a real
  `pdfium.dll` — this dev/CI environment has none (see `docs/build.md`).
  All reconstruction tests instead run against the committed
  `digikey_po_100353602.tokens.json` fixture, which was generated by
  PyMuPDF (a different text-extraction library) rather than pdfium, so it
  cannot validate pdfium's own word-grouping behavior either. First real
  validation should happen via the one `#[cfg(feature = "pdfium")]`-gated
  integration path once a `pdfium.dll` is available on a dev machine; tune
  the constants there if words are observed splitting or merging
  incorrectly.
- **DigiKey CSV/XLSX fixtures are synthesized, not real exports.**
  `crates/inventory-import/tests/fixtures/digikey_order.csv`/`.xlsx` (and
  their header-reordered/preamble variants) were built by hand from
  DigiKey's *documented* export column names, matching the real PDF
  sample's line items for cross-format consistency — but no real DigiKey
  CSV/XLSX export has been used to validate the column-alias list in
  `crate::digikey::columns::Field::aliases`. A real private export dropped
  into `samples/digikey/private/` (per `samples/digikey/README.md`) would
  let a later pass strengthen these fixtures and catch any header text this
  crate doesn't yet recognize.
- **Theoretical wrapped-description / noise-keyword collision.** Wrapped
  description continuation lines (`stitch_wrapped_description` in
  `crates/inventory-import/src/digikey/pdf.rs`) are recognized by process
  of elimination — a row is "more description" unless it matches a known
  `MFG`/noise/`TARIFF` shape. A pathological description whose wrapped
  second line happened to start with, say, the literal word "Page" (an
  `is_noise_row` marker) would be misclassified as a footer row and
  silently truncate the description instead of being stitched on. No real
  DigiKey sample has been observed to trigger this (flagged during Task 9's
  review, not from an actual failure), and the failure mode is a shortened
  `description` string, not corrupted quantities/prices/identity — but it's
  an open edge case a broader real-sample corpus could either rule out or
  need a fix for.
- **Extension-first format detection.** `detect_format`
  (`crates/inventory-import/src/parser.rs`) trusts a recognized file
  extension outright rather than cross-checking it against magic bytes; a
  file renamed to the wrong extension (an XLSX saved as `.csv`) is
  misdetected. Content sniffing only runs when the extension is missing or
  unrecognized. Deliberate (cheap, correct in the overwhelming common
  case), not an oversight — revisit if a mislabeled-upload report ever
  surfaces.

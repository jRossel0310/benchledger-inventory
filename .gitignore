# Raw supplier documents containing personal information — never commit to the public repo.
# Sanitized fixtures belong in samples/digikey/ (committed).
Sample-invoice-movearoundwhenready/
samples/digikey/private/

# Dependencies and build output
node_modules/
dist/
target/
*.tsbuildinfo

# Playwright test artifacts
test-results/
playwright-report/

# Dev-only snapshot served by `pnpm --filter @ei/web dev` — production gets
# the real one via publishing; the committed public/ stays empty of snapshots.
apps/web/public/inventory.snapshot.json

# Tauri CLI auto-generated capability/ACL schema files (regenerated on every build)
apps/desktop/src-tauri/gen/schemas/

# Local environment
.env
.env.*
*.local

# Logs and OS noise
*.log
Thumbs.db
.DS_Store

# Private PII denylist for the fixture guard test (never commit)
samples/digikey/private/pii-denylist.txt

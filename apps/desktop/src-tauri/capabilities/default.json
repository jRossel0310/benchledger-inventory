{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core event access for the main window. The close-time publish flow (Phase 6 Task 6) needs it: the Rust close guard emits close-publish-requested and the frontend dialog listens for it via @tauri-apps/api/event.",
  "windows": ["main"],
  "permissions": ["core:event:default"]
}

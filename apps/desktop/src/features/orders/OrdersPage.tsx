import { FeaturePanel } from '../shared/FeaturePanel';

export function OrdersPage() {
  return (
    <FeaturePanel
      eyebrow="Orders"
      title="Coming in Phase 5"
      description="Orders track purchases end to end: import a DigiKey order (or enter one by hand), watch it as open/expected, and receive it into stock in one action that posts the matching ledger entries. Supplier-listing price history already exists on parts — the order workflow itself arrives with Phase 5."
    />
  );
}
